# IntegratedLearner 0.99.1

- Updated package metadata for the current Bioconductor review cycle.
- Added Himel Mallick's ORCID to the `Authors@R` field in `DESCRIPTION`.
- Promoted runtime dependencies used by package functions from `Suggests` to
  `Imports`.
- Reduced `Suggests` to packages used for the vignette, examples, and tests.
- Replaced the placeholder `NEWS` file with a versioned `NEWS.md` changelog.
