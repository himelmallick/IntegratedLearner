% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IL_survival.R
\name{ILsurv}
\alias{ILsurv}
\alias{IL_survival}
\title{IntegratedLearner Survival Engine}
\usage{
ILsurv(
  feature_table,
  sample_metadata,
  feature_metadata,
  valid_feature_table = NULL,
  valid_sample_metadata = NULL,
  base_learner = "surv.rfsrc",
  folds = 5,
  seed = 123,
  verbose = FALSE,
  do_early_fusion = TRUE,
  weight_method = c("IBS", "COX"),
  t_vec = NULL,
  t_vec_probs = c(0.05, 0.25, 0.5, 0.75, 0.95),
  layer_score = c("sum", "mean", "l2"),
  eps = 1e-12,
  weight_lambda = 0.02,
  weight_penalty = c("l2_to_uniform", "entropy"),
  weight_cap = 1,
  optim_maxit_cox = 4000,
  optim_maxit_ibs = 300,
  ibs_shrink_to_uniform = 0,
  intermediate_learners = c("surv.coxph"),
  ...
)

IL_survival(
  feature_table,
  sample_metadata,
  feature_metadata,
  valid_feature_table = NULL,
  valid_sample_metadata = NULL,
  base_learner = "surv.rfsrc",
  folds = 5,
  seed = 123,
  verbose = FALSE,
  do_early_fusion = TRUE,
  weight_method = c("IBS", "COX"),
  t_vec = NULL,
  t_vec_probs = c(0.05, 0.25, 0.5, 0.75, 0.95),
  layer_score = c("sum", "mean", "l2"),
  eps = 1e-12,
  weight_lambda = 0.02,
  weight_penalty = c("l2_to_uniform", "entropy"),
  weight_cap = 1,
  optim_maxit_cox = 4000,
  optim_maxit_ibs = 300,
  ibs_shrink_to_uniform = 0,
  intermediate_learners = c("surv.coxph"),
  ...
)
}
\arguments{
\item{feature_table}{An R data frame containing multiview features (in rows)
and samples (in columns).
Column names of \code{feature_metadata} must match the row names of
\code{sample_metadata}.}

\item{sample_metadata}{An R data frame of metadata variables (in columns).
Must have a column named \code{subjectID} describing per-subject unique
identifiers. For longitudinal designs, this variable may be non-unique.
Additionally, a column named \code{Y} must be present which is the
continuous or binary outcome of interest.
Row names of \code{sample_metadata} must match the column names of
\code{feature_table}.}

\item{feature_metadata}{An R data frame of feature-specific metadata across
views (in columns) and features (in rows). Must have a column named
\code{featureID} as a unique per-feature identifier and a column named
\code{featureType} describing the source layers.
Row names of \code{feature_metadata} must match the row names of
\code{feature_table}.}

\item{valid_feature_table}{Validation feature table (features x samples).}

\item{valid_sample_metadata}{Validation sample metadata containing \code{time}
and \code{event}.}

\item{base_learner}{Survival base learner.}

\item{folds}{How many folds in the V-fold nested cross-validation? Default
is 5.}

\item{seed}{Specify the seed for reproducibility. Default is 1234.}

\item{verbose}{logical; TRUE for printing SuperLearner progress. Default FALSE.}

\item{do_early_fusion}{Logical; run early fusion model.}

\item{weight_method}{Late-fusion weighting method. Supported:
\code{"IBS"}, \code{"COX"}.}

\item{t_vec}{Optional explicit time points for COX-based late-fusion feature
construction.}

\item{t_vec_probs}{Quantiles used to derive \code{t_vec} when \code{t_vec}
is \code{NULL}.}

\item{layer_score}{Layer summary on cumhaz increments: \code{"sum"},
\code{"mean"}, or \code{"l2"}.}

\item{eps}{Numerical lower bound for survival probabilities.}

\item{weight_lambda}{COX-weight optimization regularization strength.}

\item{weight_penalty}{COX-weight optimization regularizer:
\code{"l2_to_uniform"} or \code{"entropy"}.}

\item{weight_cap}{Optional cap on any one layer weight (COX method).}

\item{optim_maxit_cox}{Maximum iterations for COX-weight optimization.}

\item{optim_maxit_ibs}{Maximum iterations for IBS-weight optimization.}

\item{ibs_shrink_to_uniform}{Optional convex shrinkage of IBS weights toward
uniform.}

\item{intermediate_learners}{Vector of learner IDs for intermediate fusion.}

\item{...}{Additional base-learner hyperparameters passed to
\code{base_learner}. You may also pass \code{model_args = list(...)} as a
named list where each entry is keyed by learner ID.}
}
\value{
List with \code{train_out} and \code{valid_out} in the same nested
format as previous survival implementations.
}
\description{
BioC-friendly survival backend used by \code{IntegratedLearner()} for
time-to-event outcomes. This preserves the historical \code{ILsurv()}
interface while using model fitting and fusion routines that do not depend on
\pkg{mlr3proba}/\pkg{mlr3extralearners}.
}
\examples{
\dontshow{if (requireNamespace("timeROC", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
sample_ids <- paste0("S", seq_len(40))
feature_ids <- c(paste0("g", seq_len(8)), paste0("m", seq_len(6)))
feature_types <- c(rep("gene", 8), rep("mirna", 6))
feature_table <- as.data.frame(
  matrix(rnorm(length(feature_ids) * length(sample_ids)),
         nrow = length(feature_ids),
         dimnames = list(feature_ids, sample_ids))
)
sample_metadata <- data.frame(
  time = rexp(length(sample_ids), rate = 0.2),
  event = rbinom(length(sample_ids), 1, 0.6),
  row.names = sample_ids
)
feature_metadata <- data.frame(
  featureID = feature_ids,
  featureType = feature_types,
  row.names = feature_ids
)
fit <- ILsurv(
  feature_table = feature_table,
  sample_metadata = sample_metadata,
  feature_metadata = feature_metadata,
  base_learner = "surv.coxph",
  folds = 2,
  do_early_fusion = FALSE,
  intermediate_learners = character(0),
  verbose = FALSE
)
names(fit$train_out)
\dontshow{\}) # examplesIf}
}
