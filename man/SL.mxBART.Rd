% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IL_Utilities.R
\name{SL.mxBART}
\alias{SL.mxBART}
\title{mxBART SuperLearner wrapper}
\usage{
SL.mxBART(
  Y,
  X,
  newX,
  family,
  obsWeights,
  id,
  sparse = FALSE,
  ntree = 50,
  ndpost = 1000,
  nskip = 100,
  keepevery = 10,
  mxps = list(list(prior = 1, df = 3, scale = 1)),
  ...
)
}
\arguments{
\item{Y}{Outcome variable.}

\item{X}{Covariate data frame (training).}

\item{newX}{Covariate data frame (prediction).}

\item{family}{A \code{\link[stats]{family}} object; typically \code{gaussian()} or \code{binomial()}.}

\item{obsWeights}{Optional observation weights (currently unused).}

\item{id}{Optional grouping id for mixed-effects BART.}

\item{sparse}{Logical; passed to \code{mxBART::mxbart}.}

\item{ntree}{Number of trees.}

\item{ndpost}{Number of posterior draws.}

\item{nskip}{Number of burn-in draws.}

\item{keepevery}{Thinning interval.}

\item{mxps}{Prior specification list passed to \code{mxBART::mxbart}.}

\item{...}{Additional arguments passed to \code{mxBART::mxbart}.}
}
\value{
A list with elements \code{pred} and \code{fit} (SuperLearner convention).
}
\description{
SuperLearner wrapper for mixed-effects BART using the \pkg{mxBART} package.
This learner is optional and requires \pkg{mxBART} to be installed.
}
\examples{
if (FALSE) {
  set.seed(1)
  X <- data.frame(x1 = rnorm(30), x2 = rnorm(30))
  Y <- X$x1 - 0.3 * X$x2 + rnorm(30, sd = 0.2)
  fit <- SL.mxBART(
    Y = Y,
    X = X,
    newX = X[1:4, , drop = FALSE],
    family = stats::gaussian(),
    obsWeights = rep(1, nrow(X)),
    id = seq_len(nrow(X)),
    ntree = 10,
    ndpost = 20,
    nskip = 10,
    keepevery = 1
  )
  head(fit$pred)
}
}
